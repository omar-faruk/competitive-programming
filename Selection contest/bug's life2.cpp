#include <algorithm>
#include <iostream>
#include <cstring>
#include <cstdlib>
#include <cstdio>
#include <string>
#include <cmath>

#define scan(a) scanf("%lld",&a)
#define scan2(a,b) scanf("%lld %lld",&a,&b)

using namespace std;

long long tc,i,bugs,total,vis[1000010],flag,a,b,c;

char connect[1000010];

int  main()
{
	scan(tc);
	
	for(c=1;c<=tc;c++)
	{
		flag=1;
		
		scan2(bugs,total);
		
		for(i=0;i<total;i++)
		{
			scan2(a,b);
			
			
		}
	}
	
	return 0; 
}
